{
  "name": "section2",
  "version": "1.0.0",
  "description": "## JavaScript  * `number`: Both floats and integers are number  * `string`: anything that's quoted (\"\", '', ``)     * `` is considerd tempelate litterals  * `boolean`: true or false      * 0 and none is `falsy` boolean value",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "lite-server"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lite-server": "^2.6.1"
  }
}
